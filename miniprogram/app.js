// import { toast } from "./utils/extendApi";
import "./utils/extendApi";
import {
  setStorage,
  getStorage,
  removeStorage,
  clearStorage,
} from "./utils/storage";

App({
  // onShow触发时机：
  // 当页面首次加载并显示时。
  // 当页面从后台切换到前台显示时。
  // 当页面通过 wx.navigateTo 或 wx.redirectTo 导航到另一个页面后，再次返回该页面时。
  async onShow() {},
});
